# CONFIG_UNUSED_KSYMS_WHITELIST — symbols kept alive for the DEEPX DX-M1
# out-of-tree NPU driver (dx_dma.ko) despite CONFIG_TRIM_UNUSED_KSYMS=y.
#
# Each entry is here because modpost named it, not because it looked plausible.
# Evidence, from the failed build of dx-driver 2.6.0 against kernel 6.18.37:
#   log.do_compile.2781313:
#     ERROR: modpost: "get_user_pages_fast" [pci_deepx/dx_dma.ko] undefined!
#     ERROR: modpost: "sched_set_normal"    [pci_deepx/dx_dma.ko] undefined!
#
# Both are EXPORT_SYMBOL_GPL in-tree (mm/gup.c:3300,
# kernel/sched/syscalls.c:878) and both were absent from the built
# Module.symvers, i.e. trimmed rather than missing. The driver's modules
# declare MODULE_LICENSE("GPL")/("GPL v2"), so the GPL-only exports are
# usable by it.
#
# DO NOT add symbols speculatively. Every line here widens the exported-symbol
# surface that TRIM_UNUSED_KSYMS exists to shrink. Add one only when modpost
# names it, and record the build log line that did.
get_user_pages_fast
sched_set_normal

# --- Added 2026-09-01 for the dx-driver v2.4.1 -> real v2.6.0 repin ---------
#
# The old pin (SRCREV c05be168…, mislabelled 2.6.0) needed only the two symbols
# above. Real v2.6.0 adds dx_link_health.c — PCIe link-health monitoring and
# auto-recovery — and its five new symbol requirements are exactly what that
# code would need: read link status, then reset the endpoint or the bus.
#
# Named by modpost, not guessed. Verbatim from
# .../dx-driver/2.6.0/temp/log.do_compile.1439078:
#
#   ERROR: modpost: "arm_smccc_1_1_get_conduit"  [pci_deepx/dx_dma.ko] undefined!
#   ERROR: modpost: "pci_probe_reset_bus"        [pci_deepx/dx_dma.ko] undefined!
#   ERROR: modpost: "pcie_flr"                   [pci_deepx/dx_dma.ko] undefined!
#   ERROR: modpost: "driver_for_each_device"     [pci_deepx/dx_dma.ko] undefined!
#   ERROR: modpost: "pcie_capability_read_dword" [pci_deepx/dx_dma.ko] undefined!
#
# Five undefined, all in dx_dma.ko, no others. Each confirmed to be a genuine
# in-tree export — i.e. trimmed by TRIM_UNUSED_KSYMS rather than absent —
# against this kernel's own source tree:
#
#   arm_smccc_1_1_get_conduit   EXPORT_SYMBOL_GPL  drivers/firmware/smccc/smccc.c:51
#   driver_for_each_device      EXPORT_SYMBOL_GPL  drivers/base/driver.c:134
#   pcie_capability_read_dword  EXPORT_SYMBOL      drivers/pci/access.c:488
#   pcie_flr                    EXPORT_SYMBOL_GPL  drivers/pci/pci.c:4494
#   pci_probe_reset_bus         EXPORT_SYMBOL_GPL  drivers/pci/pci.c:5727
#
# Four are GPL-only; dx_dma declares MODULE_LICENSE("GPL"), so they are usable
# by it.
#
# COST NOTE: editing this file changes the kernel's do_configure signature, so
# it forces a full kernel rebuild — and therefore a new vermagic and a new
# autogenerated module-signing key. Kernel and modules are only valid as a
# matched pair from one build, so the resulting image must be flashed whole.
arm_smccc_1_1_get_conduit
driver_for_each_device
pcie_capability_read_dword
pcie_flr
pci_probe_reset_bus
